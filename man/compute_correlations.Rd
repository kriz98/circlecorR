% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_correlations.R
\name{compute_correlations}
\alias{compute_correlations}
\title{Compute a correlation matrix and (raw) p-values from raw data}
\usage{
compute_correlations(
  data,
  vars = NULL,
  method = c("pearson", "spearman", "kendall"),
  use = "pairwise.complete.obs"
)
}
\arguments{
\item{data}{A data frame or matrix of observations (rows) by variables
(columns).}

\item{vars}{Optional character vector selecting and ordering the columns of
\code{data} to use. Defaults to all columns.}

\item{method}{Correlation method, passed to \code{\link[psych:corr.test]{psych::corr.test()}}. One of
\code{"pearson"}, \code{"spearman"}, \code{"kendall"}.}

\item{use}{Handling of missing values, passed to \code{\link[psych:corr.test]{psych::corr.test()}}.}
}
\value{
An object of class \code{"circlecor"}: a list with elements \code{r} (the
correlation matrix), \code{p} (a symmetric matrix of \strong{raw}, unadjusted
p-values), \code{n} (the pairwise sample sizes from \code{psych}), and \code{method}.
}
\description{
A thin wrapper around \code{\link[psych:corr.test]{psych::corr.test()}} that returns the correlation
(\code{r}) and \strong{unadjusted} p-value (\code{p}) matrices bundled in a single object
suitable for passing straight to \code{\link[=corr_wheel]{corr_wheel()}}.
}
\details{
Multiple-comparison adjustment is deliberately \strong{not} applied here. On a
correlation wheel, self-correlations and (usually) within-category
correlations are never tested, so they should not count towards the family
of comparisons. \code{\link[=corr_wheel]{corr_wheel()}} therefore applies the adjustment itself, over
exactly the set of correlations it displays -- which is both statistically
consistent and more powerful than adjusting across the full matrix. See the
\code{adjust} and \code{hide_within_group} arguments of \code{\link[=corr_wheel]{corr_wheel()}}.
}
\examples{
cc <- compute_correlations(mtcars, method = "pearson")
str(cc)
}
\seealso{
\code{\link[=corr_wheel]{corr_wheel()}}
}
