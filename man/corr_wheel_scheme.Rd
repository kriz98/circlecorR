% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schemes.R
\name{corr_wheel_scheme}
\alias{corr_wheel_scheme}
\title{Get a built-in colour scheme}
\usage{
corr_wheel_scheme(name = "default")
}
\arguments{
\item{name}{A scheme name; see \code{\link[=corr_wheel_schemes]{corr_wheel_schemes()}} for the available
options.}
}
\value{
A list with elements \code{colors} (an unnamed vector of category
colours, cycled across however many categories are plotted) and
\code{palette} (a length-3 diverging colour vector: negative, midpoint,
positive).
}
\description{
Returns the category-colour and diverging-link-palette definition for a
built-in scheme, so you can inspect it or tweak a copy before passing it to
\code{\link[=corr_wheel]{corr_wheel()}}'s \code{scheme} argument.
}
\examples{
s <- corr_wheel_scheme("colorblind")
s$palette

# Tweak a copy and use it
s$palette[2] <- "grey95"
data(gastro_symptoms)
grp <- list(Demographics = c("Age", "BMI"),
           Metrics = c("Amplitude", "Fed-Fasted AR", "Frequency", "GA-RI"))
if (requireNamespace("psych", quietly = TRUE)) {
  corr_wheel(gastro_symptoms, groups = grp, scheme = s, r_threshold = 0)
}
}
\seealso{
\code{\link[=corr_wheel_schemes]{corr_wheel_schemes()}}, \code{\link[=corr_wheel]{corr_wheel()}}
}
